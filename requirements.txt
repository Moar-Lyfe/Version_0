# Executive Dashboard -- runtime dependencies
# Pinned to minimum-known-good versions; upper bounds keep the UI stable.
streamlit>=1.40,<2.0
pandas>=2.1,<3.0
openpyxl>=3.1,<4.0     # .xlsx / .xlsm reader
PyYAML>=6.0,<7.0
